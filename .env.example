# Briefapp.Zap — Environment Variables
# Copy this file to .env and fill in your values

# Server port (default: 3000)
PORT=3000

# Gemini API Key (optional — can also be set via the Settings UI)
GEMINI_API_KEY=

# Encryption key for storing API keys (auto-generated if not set)
ENCRYPTION_KEY=
